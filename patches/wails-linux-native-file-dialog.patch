--- a/vendor/github.com/wailsapp/wails/v2/internal/frontend/desktop/linux/window.c
+++ b/vendor/github.com/wailsapp/wails/v2/internal/frontend/desktop/linux/window.c
@@ -765,10 +765,8 @@
     {
         label = "_Save";
     }
-    GtkWidget *dlgWidget = gtk_file_chooser_dialog_new(options->title, options->window, options->action,
-                                                       "_Cancel", GTK_RESPONSE_CANCEL,
-                                                       label, GTK_RESPONSE_ACCEPT,
-                                                       NULL);
+    GtkFileChooserNative *dlgWidget = gtk_file_chooser_native_new(options->title, options->window, options->action,
+                                                                   label, "_Cancel");
 
     GtkFileChooser *fc = GTK_FILE_CHOOSER(dlgWidget);
     // filters
@@ -815,7 +813,7 @@
         }
     }
 
-    gint response = gtk_dialog_run(GTK_DIALOG(dlgWidget));
+    gint response = gtk_native_dialog_run(GTK_NATIVE_DIALOG(dlgWidget));
 
     // Max 1024 files to select
     char **result = calloc(1024, sizeof(char *));
@@ -861,7 +859,7 @@
         }
         freeFileFilterArray(options->filters);
     }
-    gtk_widget_destroy(dlgWidget);
+    g_object_unref(dlgWidget);
     free(options->title);
 }
 
